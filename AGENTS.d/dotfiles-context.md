# dotfiles 特定上下文（扩展）

* 项目类型：个人开发环境配置（dotfiles），主要管理 shell、编辑器、Git、Homebrew 等工具的配置文件和安装脚本。
* 运行环境：优先针对 macOS（配合 Homebrew、zsh、tmux、vim 等），兼顾类 Unix 环境的可移植性。
* 允许修改：本仓库中如 `Brewfile`、`shell*`、`zsh*` 等环境配置文件可以根据当前机器与技术栈调整，只要变更保持幂等且不泄露敏感信息。
* 禁止内容：不在本仓库存放业务代码、生产密钥或任何受合规约束的机密，只保存开发环境相关的公共配置与脚本。

